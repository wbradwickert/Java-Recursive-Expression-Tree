# Java-Recursive-Expression-Tree
Java project I did in university. It's an algorithmic parser that converts postfix mathematical expressions into a binary tree structure using recursion and Stack logic.
